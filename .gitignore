# Pinned checkouts and build trees, all reproducible from pins.json via `bench.py setup`.
/.build/
/rust-runner/target/
/results/raw/
__pycache__/
# Scale-tier fixtures: 30 MB each and deterministic, so regenerate rather than store.
/fixtures/scale/
