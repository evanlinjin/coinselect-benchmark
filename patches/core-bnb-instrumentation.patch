diff --git a/src/wallet/coinselection.cpp b/src/wallet/coinselection.cpp
index d7fde38..54e5a04 100644
--- a/src/wallet/coinselection.cpp
+++ b/src/wallet/coinselection.cpp
@@ -18,6 +18,13 @@
 #include <queue>
 
 namespace wallet {
+// Benchmark instrumentation (coinselect-benchmark, see patches/README.md). SelectCoinsBnB is the
+// only search here that records neither its node count nor whether it finished, and both are
+// dropped entirely on the paths that return util::Error. These globals observe the existing loop
+// counter; nothing about the search itself changes.
+size_t g_bnb_selections_evaluated{0};
+bool g_bnb_algo_completed{true};
+
 // Common selection error across the algorithms
 static util::Result<SelectionResult> ErrorMaxWeightExceeded()
 {
@@ -93,6 +100,10 @@ static const size_t TOTAL_TRIES = 100000;
 util::Result<SelectionResult> SelectCoinsBnB(std::vector<OutputGroup>& utxo_pool, const CAmount& selection_target, const CAmount& cost_of_change,
                                              int max_selection_weight)
 {
+    // Reset first so an early return below still reports this call rather than the last one.
+    g_bnb_selections_evaluated = 0;
+    g_bnb_algo_completed = true;
+
     SelectionResult result(selection_target, SelectionAlgorithm::BNB);
     CAmount curr_value = 0;
     std::vector<size_t> curr_selection; // selected utxo indexes
@@ -121,7 +132,8 @@ util::Result<SelectionResult> SelectCoinsBnB(std::vector<OutputGroup>& utxo_pool
     bool max_tx_weight_exceeded = false;
 
     // Depth First search loop for choosing the UTXOs
-    for (size_t curr_try = 0, utxo_pool_index = 0; curr_try < TOTAL_TRIES; ++curr_try, ++utxo_pool_index) {
+    size_t curr_try = 0;
+    for (size_t utxo_pool_index = 0; curr_try < TOTAL_TRIES; ++curr_try, ++utxo_pool_index) {
         // Conditions for starting a backtrack
         bool backtrack = false;
         if (curr_value + curr_available_value < selection_target || // Cannot possibly reach target with the amount remaining in the curr_available_value.
@@ -185,6 +197,9 @@ util::Result<SelectionResult> SelectCoinsBnB(std::vector<OutputGroup>& utxo_pool
         }
     }
 
+    g_bnb_selections_evaluated = curr_try;
+    g_bnb_algo_completed = curr_try < TOTAL_TRIES;
+
     // Check for solution
     if (best_selection.empty()) {
         return max_tx_weight_exceeded ? ErrorMaxWeightExceeded() : util::Error();
